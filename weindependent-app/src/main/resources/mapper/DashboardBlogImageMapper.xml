<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weindependent.app.database.mapper.weindependent.BlogImageMapper">
    <select id="query" parameterType="map" resultType="com.weindependent.app.database.dataobject.BlogImageDO">
        SELECT * FROM blog_image
      <where>
          <if test="category!=null and category!=''">
              AND category = #{category}
          </if>
          <if test="fileName!=null and fileName!=''">
              AND file_name = #{fileName}
          </if>
          <if test="fileType!=null and fileType!=''">
              AND file_type = #{fileType}
          </if>
          <if test="filePath!=null and filePath!=''">
              AND file_path = #{filePath}
          </if>
          <if test="isDeleted!=null">
              is_deleted = #{isDeleted}
          </if>
      </where>

    </select>

    <select id="findById" parameterType="string" resultType="com.weindependent.app.database.dataobject.BlogImageDO">
        SELECT * FROM blog_image WHERE id = #{id}
    </select>

    <select id="getAllImages" resultType="com.weindependent.app.database.dataobject.UserDO">
        SELECT * FROM blog_image
    </select>

    <insert id="insert" parameterType="com.weindependent.app.database.dataobject.BlogImageDO" useGeneratedKeys="true" keyProperty="id">>
        INSERT INTO blog_image (category, file_name, file_type, file_path)
        VALUES (#{category}, #{fileName}, #{fileType}, #{filePath})
    </insert>

    <update id="update" parameterType="map">
        UPDATE blog_image
        <trim prefix="SET" suffixOverrides=",">
            <if test="category!=null and category!=''">
                category = #{category}
            </if>
            <if test="fileName!=null and fileName!=''">
                file_name = #{fileName}
            </if>
            <if test="fileType!=null and fileType!=''">
                file_type = #{fileType}
            </if>
            <if test="filePath!=null and filePath!=''">
                file_path = #{filePath}
            </if>
            <if test="isDeleted!=null">
                is_deleted = #{isDeleted}
            </if>
            <if test="updateTime!=null">
                update_time = #{updateTime}
            </if>
        </trim>
        <where>
            <if test="category!=null and category!=''">
                AND category = #{category}
            </if>
            <if test="fileName!=null and fileName!=''">
                AND file_name = #{fileName}
            </if>
            <if test="fileType!=null and fileType!=''">
                AND file_type = #{fileType}
            </if>
            <if test="filePath!=null and filePath!=''">
                AND file_path = #{filePath}
            </if>
            <if test="isDeleted!=null">
                AND is_deleted = #{isDeleted}
            </if>
        </where>
    </update>
</mapper>

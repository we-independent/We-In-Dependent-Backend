<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weindependent.app.database.mapper.dashboard.DashboardNotificationBroadcastImageMapper">

    <insert id="insert" parameterType="NotificationBroadcastImageDO">
        INSERT INTO notification_broadcast_image
        (message_id, image_url, image_type, caption, button_text, button_url, sort_order)
        VALUES
        (#{messageId}, #{imageUrl}, #{imageType}, #{caption}, #{buttonText}, #{buttonUrl}, #{sortOrder})
    </insert>

    <delete id="delete" parameterType="long">
        DELETE FROM notification_broadcast_image WHERE id = #{id}
    </delete>

    <select id="listByMessageId" resultType="NotificationBroadcastImageDO">
        SELECT * FROM notification_broadcast_image
        WHERE message_id = #{messageId}
        ORDER BY sort_order ASC, id ASC
    </select>

</mapper>